# ============================================================
#  OKXB dependencies  (Python 3.11+ recommended)
#  install:  py -m venv .venv  &&  .venv\Scripts\activate  &&  pip install -r requirements.txt
# ============================================================

# --- OKX 官方 SDK + 网络 ---
python-okx>=0.3.9          # 官方 V5 SDK (REST + WS)
websockets>=12.0           # 备用/直连 WS
httpx>=0.27.0              # EDGAR / Finnhub / 通用异步 HTTP
requests>=2.31.0          # verify_account.py 独立签名请求用

# --- 配置 / 校验 / 日志 ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
PyYAML>=6.0
python-dotenv>=1.0.0
structlog>=24.1.0

# --- 数据 / 数值 / 建模 ---
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
scikit-learn>=1.4.0
lightgbm>=4.3.0

# --- v2 正交数据源 (免费) ---
coinmetrics-api-client>=2024.1.0   # community (no-key) daily on-chain metrics
pandas-datareader>=0.10.0          # macro via stooq (VIX / indices / yields)

# --- 数据结构 ---
sortedcontainers>=2.4.0    # 本地订单簿有序价位

# --- 存储 ---
aiosqlite>=0.20.0

# --- AI 事件分类 ---
anthropic>=0.40.0

# --- 时间同步检查 ---
ntplib>=0.4.0

# --- GUI 与打包 ---
customtkinter>=5.2.2       # 现代深色 GUI
pyinstaller>=6.3.0         # 打包成 .exe

# --- 测试 ---
pytest>=8.0.0
pytest-asyncio>=0.23.0

# --- 可选: 提速 (因子热路径) ---
# numba>=0.59.0
